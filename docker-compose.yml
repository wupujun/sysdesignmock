services:
  sysdesignmock:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
      WHITEBOARD_DATA_DIR: /app/data
    volumes:
      - sysdesignmock-data:/app/data
    restart: unless-stopped

volumes:
  sysdesignmock-data:
