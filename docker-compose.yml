services:
  sysdesignmock:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      WHITEBOARD_DATA_DIR: /app/data
    volumes:
      - sysdesignmock-data:/app/data
    restart: unless-stopped

volumes:
  sysdesignmock-data:
